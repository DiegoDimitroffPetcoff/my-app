

export class User {
  firstName = "";
  lastName = "";
  email = "";
  connection = "";
  

  constructor(firstName, lastName, email, connection) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
    this.connection = connection;
  }
}
